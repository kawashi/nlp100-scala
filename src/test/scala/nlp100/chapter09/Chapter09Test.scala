package nlp100.chapter09

import org.scalatest.{FunSuite, Matchers}

/**
  * 第9章: ベクトル空間法 (I)
  */
class Chapter09Test extends FunSuite with Matchers {

  override def suiteName: String = "第9章: ベクトル空間法 (I)"

  test("80. コーパスの整形") {
    fail()
  }

  test("81. 複合語からなる国名への対処") {
    fail()
  }

  test("82. 文脈の抽出") {
    fail()
  }

  test("83. 単語／文脈の頻度の計測") {
    fail()
  }

  test("84. 単語文脈行列の作成") {
    fail()
  }

  test("85. 主成分分析による次元圧縮") {
    fail()
  }

  test("86. 単語ベクトルの表示") {
    fail()
  }

  test("87. 単語の類似度") {
    fail()
  }

  test("88. 類似度の高い単語10件") {
    fail()
  }

  test("89. 加法構成性によるアナロジー") {
    fail()
  }
}