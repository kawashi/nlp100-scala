package nlp100.chapter05

import org.scalatest.{FunSuite, Matchers}

/**
  * 第5章: 係り受け解析
  */
class Chapter05Test extends FunSuite with Matchers {

  override def suiteName: String = "第5章: 係り受け解析"

  test("40. 係り受け解析結果の読み込み（形態素）") {
    fail()
  }

  test("41. 係り受け解析結果の読み込み（文節・係り受け）") {
    fail()
  }

  test("42. 係り元と係り先の文節の表示") {
    fail()
  }

  test("43. 名詞を含む文節が動詞を含む文節に係るものを抽出") {
    fail()
  }

  test("44. 係り受け木の可視化") {
    fail()
  }

  test("45. 動詞の格パターンの抽出") {
    fail()
  }

  test("46. 動詞の格フレーム情報の抽出") {
    fail()
  }

  test("47. 機能動詞構文のマイニング") {
    fail()
  }

  test("48. 名詞から根へのパスの抽出") {
    fail()
  }

  test("49. 名詞間の係り受けパスの抽出") {
    fail()
  }
}